# 共享精确前缀流水线的独立只读审计

本次只读审阅 `generate_exact_prefix_family.py` 与 `verify_exact_prefix_family_parallel.py`，没有重新编译或修改它们。脚本源码哈希和审计时间见同名 JSON。

五组目标精确划分冻结的 `E16-after-selected-cores16.json`，其 SHA-256 为 `5ebf07406632fae1324eb30c76c102ea766bca397e306bb97374fd6bffc165de`；无遗漏、无组间重复。总计 16 个目标、12180 个状态、8750 个 gain 停止节点。逐数据检查，所有非停止节点都选用 SlackSafe。

| 组 | 目标数 | 状态数 | 停止节点 |
| --- | ---: | ---: | ---: |
| Q11，Wcap=33 | 2 | 766 | 660 |
| Q10，Wcap=33 | 7 | 1843 | 1538 |
| Q10，Wcap=34 | 1 | 3415 | 2022 |
| Q7，Wcap=33 | 5 | 2223 | 1959 |
| Q7，Wcap=34 | 1 | 3933 | 2571 |

Python 的状态生成和停止计算是候选数据。每个停止节点分别产生非空性与 gainTest 的 kernel 证明；每个非停止节点核验整数缓存及原 FamilyEntrySafe 的完整相关后继。CanExtend 只允许省略不能被任一目标容纳的配额，已证 Fits 不变量负责对每个实际目标的后继覆盖。最终 quota_coverage 又在 Lean 内核中核验目标的配额包含、相同初态、非空、权重上界与商值下界。

最终生成结论仅为 ExactPrefixSafe；没有将其伪装成旧 Safe，实际集合桥仍需要精确余量条件。生成器的 Python 断言可以使生成提前失败，不能使错误数据获得证明。

runner 只有在进程真实退出 0、公理报告属于标准集合、对象存在且源码/依赖哈希未变时记录通过。缓存复用还核对本地导入对象及日志哈希。每个分块绑定 Data 对象；最终装配绑定所有分块对象，最终汇总再次检查已完成对象和日志。停止请求的进程返回 0 不代表完整通过，因为 manifest 保持缺项而不会设置总 pass。

未发现语义或通过状态判断缺口。本审计本身不宣称这五组具体 Lean 证明均已完成；其完成状态由各自实际编译 manifest 决定。

调度补充审查：runner 新增 `--data-slots`，允许高内存 Data 编译占用两个全局槽。参数限制在池容量以内；只有获得完整请求数量的锁才启动进程，否则释放全部已获锁再重试，避免部分持锁等待造成死锁。证明分块仍各占一槽。真实退出码、公理、源码与对象校验和最终通过条件均未改。新旧 runner 哈希分别保留于审计 JSON。此调度变化不修改数学规则。
